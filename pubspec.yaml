name: extended_http
description: It extends HTTP package then combine with Hive to provide cache. It also supports authorization via custom headers.
version: 0.1.4
homepage: https://github.com/ngoan98tv/extended_http

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
